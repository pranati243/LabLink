# LabLink Docker Environment Configuration
# This file contains default values for Docker Compose deployment
# Copy this file to .env and modify values as needed

# =============================================================================
# Flask Application Settings
# =============================================================================

# Flask environment: development, production, or testing
FLASK_ENV=development

# Secret key for Flask session management (CHANGE IN PRODUCTION!)
SECRET_KEY=dev-secret-key-change-in-production

# =============================================================================
# JWT Authentication Settings
# =============================================================================

# Secret key for JWT token signing (CHANGE IN PRODUCTION!)
JWT_SECRET_KEY=dev-jwt-secret-change-in-production

# JWT access token expiration in hours (default: 1 hour)
JWT_ACCESS_TOKEN_HOURS=1

# JWT refresh token expiration in days (default: 30 days)
JWT_REFRESH_TOKEN_DAYS=30

# =============================================================================
# Database Settings (PostgreSQL)
# =============================================================================

# PostgreSQL database name
POSTGRES_DB=lablink

# PostgreSQL username
POSTGRES_USER=lablink_user

# PostgreSQL password (CHANGE IN PRODUCTION!)
POSTGRES_PASSWORD=lablink_password

# PostgreSQL port (default: 5432)
POSTGRES_PORT=5432

# Database connection pool settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_RECYCLE=3600

# =============================================================================
# CORS Settings
# =============================================================================

# Allowed CORS origins (comma-separated)
# Use * for development, specific domains for production
# Example: http://localhost:3000,https://yourdomain.com
CORS_ORIGINS=*

# =============================================================================
# Application Port Settings
# =============================================================================

# Backend API port (default: 5000)
BACKEND_PORT=5000

# =============================================================================
# AWS Settings (Optional - for production deployment)
# =============================================================================

# AWS S3 bucket for component images (optional)
# AWS_S3_BUCKET=lablink-images

# AWS access credentials (optional)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# AWS region (optional)
# AWS_REGION=us-east-1

# =============================================================================
# Production Deployment Notes
# =============================================================================
#
# For production deployment, ensure you:
# 1. Generate strong random values for SECRET_KEY and JWT_SECRET_KEY
#    Example: python -c "import secrets; print(secrets.token_hex(32))"
#
# 2. Use a strong database password
#
# 3. Set FLASK_ENV=production
#
# 4. Configure specific CORS_ORIGINS (not *)
#
# 5. Consider using AWS RDS for the database instead of the Docker container
#    Update DATABASE_URL in docker-compose.yml accordingly
#
# 6. Set up SSL/TLS certificates for HTTPS
#
# 7. Configure proper firewall rules and security groups
#
# 8. Enable database backups
#
# 9. Set up monitoring and logging
#
# 10. Review and harden all security settings
#
# =============================================================================
