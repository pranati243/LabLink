# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/lablink

# JWT Configuration
JWT_SECRET_KEY=your-secret-key-here-change-in-production

# Flask Configuration
FLASK_ENV=development
FLASK_APP=app.py

# AWS S3 Configuration (Optional - for storing component images)
# Leave commented out to use local file storage
# Uncomment and configure to enable S3 storage
# See AWS_S3_INTEGRATION.md for detailed setup instructions

# S3 bucket name (must be globally unique)
# AWS_S3_BUCKET=lablink-images-prod-2024

# AWS region where bucket is located
# AWS_REGION=us-east-1

# IAM User credentials (only needed if not using IAM role)
# If running on EC2 with IAM role attached, leave these commented out
# AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
# AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# Optional: CloudFront CDN domain for faster image delivery
# CLOUDFRONT_DOMAIN=d1234567890.cloudfront.net

# Application Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5000
